apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: customer-namespace
  labels:
    app: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
